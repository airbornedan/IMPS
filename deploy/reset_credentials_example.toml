# COPY TO /var/www/getimps.com/demo/deploy/reset_credentials.toml,
# EDIT, THEN:
#
#   chmod 600 /var/www/getimps.com/demo/deploy/reset_credentials.toml
#   chown www-data:www-data /var/www/getimps.com/demo/deploy/reset_credentials.toml
#
# (Must match imps_reset.py's RESET_CONFIG_FILE constant exactly --
# a previous version of this comment pointed at /var/www/deploy/,
# which imps_reset.py never actually reads from.)

[reset_database]
user = "box_reset"
password = "box_reset_password"
host = "localhost"
