[Unit]
Description=Run IMPS demo reset on a schedule

[Timer]
# Every 10 minutes. (Previously every 6 hours -- OnCalendar=*-*-*
# 00,06,12,18:00:00 -- which didn't match the intended cadence.)
OnCalendar=*:0/10
Persistent=true
RandomizedDelaySec=10

[Install]
WantedBy=timers.target
